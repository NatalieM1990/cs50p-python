# Ask the user to type something, and store what they typed in a variable called "text"
text = input("Type Something: ")

# Convert the text to lowercase, then display it on screen
print(text.replace(" ", "..."))